from effects.mystic_portal import draw_mystic_portal

def execute_portal(frame, center, radius, rotation_angle):
    """Execute the Mystic Portal visual effect."""

    draw_mystic_portal(
        frame,
        center,
        radius,
        rotation_angle
    )